# Список файлов для переноса в другой проект

## Обязательные файлы

### 1. Компоненты (src/components/)
- ✅ `ChatButton.tsx` - Анимированная кнопка чата
- ✅ `PopUp.tsx` - Всплывающее окно
- ✅ `PaymentButton.tsx` - Кнопка оплаты с Stripe
- ✅ `ui/button.tsx` - UI компонент кнопки
- ✅ `ui/dialog.tsx` - UI компонент диалога

### 2. Контексты (src/contexts/)
- ✅ `PopUpContext.tsx` - Контекст для управления состоянием popup

### 3. Утилиты (src/lib/)
- ✅ `cookie.ts` - Функции для работы с cookies
- ✅ `utils.ts` - Вспомогательные функции (cn)

### 4. Стили (src/styles/)
- ✅ `widget.css` - Все стили и анимации виджета

### 5. Конфигурация
- ✅ `tailwind.config.js` - Конфигурация Tailwind (для справки)
- ✅ `package.json` - Список зависимостей

### 6. Документация
- ✅ `README.md` - Основная документация
- ✅ `INTEGRATION_GUIDE.md` - Подробное руководство по интеграции
- ✅ `FILES_TO_COPY.md` - Этот файл

### 7. Точка входа
- ✅ `src/index.ts` - Экспорты всех компонентов

## Опциональные файлы

### Изображения
Если используете аватар из текущего проекта:
- `client/src/assets/georgie.jpg` - Аватар для popup

## Структура для копирования

```
Скопируйте эту структуру в ваш проект:

your-project/
└── src/
    └── popup-widget/          # Создайте эту папку
        ├── components/
        │   ├── ChatButton.tsx
        │   ├── PopUp.tsx
        │   ├── PaymentButton.tsx
        │   └── ui/
        │       ├── button.tsx
        │       └── dialog.tsx
        ├── contexts/
        │   └── PopUpContext.tsx
        ├── lib/
        │   ├── cookie.ts
        │   └── utils.ts
        ├── styles/
        │   └── widget.css
        └── index.ts
```

## Зависимости для установки

Выполните эту команду в вашем проекте:

```bash
npm install @radix-ui/react-dialog@^1.1.2 @radix-ui/react-slot@^1.1.0 @radix-ui/react-toast@^1.2.2 @stripe/react-stripe-js@^3.6.0 @stripe/stripe-js@^7.0.0 class-variance-authority@^0.7.0 clsx@^2.1.1 lucide-react@^0.453.0 tailwind-merge@^2.5.4 tailwindcss-animate
```

## Быстрый чеклист интеграции

- [ ] Скопировал все файлы из `src/` в `your-project/src/popup-widget/`
- [ ] Установил зависимости командой выше
- [ ] Обновил `tailwind.config.js` (добавил путь к виджету и расширил тему)
- [ ] Добавил CSS переменные в главный CSS файл
- [ ] Импортировал `widget.css` в главный CSS файл
- [ ] Настроил backend endpoint `/api/create-payment-intent` для Stripe
- [ ] Добавил Stripe ключи в `.env`
- [ ] Обернул приложение в `<PopUpProvider>`
- [ ] Добавил `<ChatButton />` и `<PopUp />` в приложение
- [ ] Протестировал работу виджета

## Минимальный пример интеграции

```tsx
// src/App.tsx
import { PopUpProvider } from './popup-widget/contexts/PopUpContext';
import ChatButton from './popup-widget/components/ChatButton';
import PopUp from './popup-widget/components/PopUp';
import { usePopUp } from './popup-widget/contexts/PopUpContext';
import './popup-widget/styles/widget.css';

function PopUpWidget() {
  const { popupContent } = usePopUp();
  return (
    <PopUp
      title={popupContent.title}
      description={popupContent.description}
      price={popupContent.price}
      discount={popupContent.discount}
      imageSrc={popupContent.imageSrc}
      amount={popupContent.amount}
    />
  );
}

function App() {
  return (
    <PopUpProvider>
      <div className="App">
        {/* Ваш контент */}
        <h1>Мое приложение</h1>
        
        {/* Виджет */}
        <ChatButton />
        <PopUpWidget />
      </div>
    </PopUpProvider>
  );
}

export default App;
```

## Размер пакета

Общий размер: ~50KB (без зависимостей)
- Компоненты: ~30KB
- Стили: ~5KB
- Утилиты: ~2KB
- Остальное: ~13KB

## Что НЕ нужно копировать

❌ `node_modules/` - установите зависимости через npm
❌ `.git/` - история git не нужна
❌ `dist/` - будет создана при сборке
❌ Тестовые файлы (если есть)

## Следующие шаги

1. Прочитайте `INTEGRATION_GUIDE.md` для детальной инструкции
2. Следуйте чеклисту выше
3. Кастомизируйте контент под ваши нужды
4. Настройте Stripe для приема платежей

Готово! 🎉
